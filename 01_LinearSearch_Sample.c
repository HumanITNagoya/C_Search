#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
  /* 変数宣言 */
  int i;
  int tbl[10];
  
  /* ランダム関数の初期化 */
  srand((unsigned)time(NULL));
  
  /* 0～99までのランダムな数値を配列に格納 */
  
  
  return 0;
}
